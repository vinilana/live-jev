{
  "name": "live-jev-self-driving",
  "version": "1.0.0",
  "description": "2D autonomous car simulation that uses TypeSafe's Jev (System One) as its decision classifier",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "sim:headless": "node scripts/headless.js"
  },
  "engines": { "node": ">=20" },
  "dependencies": {
    "@typesafe-ai/sdk": "^0.6.0"
  }
}
